#include <Arduino.h>



//SnowSparkle(0, 0xFF, 0, 20, 20);
void SnowSparkle(byte red, byte green, byte blue, int SparkleDelay, int SpeedDelay, int num_led) {
  /*
  int RandomLight[40];

  for (int i = 0; i <= num_led; i++) {
    leds[matrixSparkle[i]] = 0x000900;
  }
  FastLED.show();

  for (size_t i = 0; i < num_led/2; i++)
  {
    RandomLight[i] = random(num_led);
  }

  for (size_t i = 0; i < num_led/2; i++)
  {
    leds[matrixSparkle[RandomLight[i]]] = 0xFFFFFF;    
  }
  FastLED.show();
  delay(SparkleDelay);  
  for (size_t i = 0; i < num_led/2; i++)
  {
    leds[matrixSparkle[RandomLight[i]]] = 0x000900;     
  }
  FastLED.show();
  delay(SpeedDelay);
  */
}